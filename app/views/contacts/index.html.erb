<% provide(:title, 'Your contacts') %>
<h1>Your contacts</h1>
<div class="row">
	  <div class="span6">
<%= will_paginate %>

<ul class="contacts">
  <% current_user.contacts.each do |contact| %>
    <%= render contact %>
  <% end %>
</ul>

<%= will_paginate %>
</div>
</div>
